<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Movies" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/gray_main</item>
    </style>

    <style name="Theme.Movies" parent="Base.Theme.Movies" />

    <style name="ItemActiveNavigation" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@color/edit_search</item>
    </style>

    <style name="fontTabLayout" parent="Widget.Material3.TabLayout">
        <item name="android:fontFamily">@font/poppins_medium</item>
        <item name="android:textSize">14sp</item>
    </style>
</resources>